SUPABASE_URL=https://pimhhbhhmwqvgmwrqjsx.supabase.co
# Client-safe Supabase publishable/anon key; never use the server secret here.
SUPABASE_PUBLISHABLE_KEY=
# Server-only Supabase secret/service-role key. Keep in an ignored local env or secret manager.
SUPABASE_SECRET_KEY=
SUPABASE_REALTIME_SCHEMA=public
SUPABASE_REALTIME_TABLE=participants
# Must be a unique random secret of at least 32 bytes and match the registration service.
# Do not put this value in browser code or commit it.
INTERNAL_API_SECRET=
# Optional exact origins, comma-separated. Leave blank for same-origin-only use.
CORS_ALLOWED_ORIGINS=
# Database writes from /api/qr/generate are disabled unless explicitly enabled.
QR_GENERATION_PERSIST_ENABLED=false
